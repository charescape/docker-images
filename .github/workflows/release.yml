name: release

on:
  push:
    branches:
      - "**"

jobs:
  build:
    name: Build
    if: "contains(github.event.head_commit.message, 'release ')"

    runs-on: ubuntu-24.04

    env:
      GIT_MSG: ${{ github.event.head_commit.message }}

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v7

      - name: Parse release path
        id: parse
        run: |
          set -eu
          REST=$(printf '%s\n' "$GIT_MSG" | awk '{
            for (i = 1; i <= NF; i++) {
              if ($i == "release") {
                print $(i + 1), $(i + 2)
                exit
              }
            }
          }')
          CODE_PATH=$(printf '%s\n' "$REST" | awk '{print $1}')
          VER=$(printf '%s\n' "$REST" | awk '{print $2}')

          if [ -z "$CODE_PATH" ]; then
            echo "No release token found; skip"
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if ! printf '%s\n' "$CODE_PATH" | grep -qE '^[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+$'; then
            echo "Not a four-segment path ($CODE_PATH); skip"
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          case "/$CODE_PATH/" in
            */./*|*/../*)
              echo "Invalid path segment in $CODE_PATH"
              exit 1
              ;;
          esac

          if ! printf '%s\n' "$VER" | grep -qE '^v[0-9]+\.[0-9]+$'; then
            echo "Four-segment path requires v<X.Y> (got: ${VER:-empty})"
            exit 1
          fi

          if [ ! -f "${CODE_PATH}/Dockerfile" ]; then
            echo "Missing ${CODE_PATH}/Dockerfile"
            exit 1
          fi

          IMAGE_TAG=$(printf '%s-%s\n' "$(printf '%s\n' "$CODE_PATH" | tr '/' '-')" "$VER" | tr '[:upper:]' '[:lower:]')
          GHCR_IMAGE_ID=$(printf 'ghcr.io/%s/docker-images:%s\n' "${{ github.repository_owner }}" "$IMAGE_TAG" | tr '[:upper:]' '[:lower:]')
          DOCKERHUB_USER=$(printf '%s\n' "${{ secrets.DOCHUB_USERNAME }}" | tr '[:upper:]' '[:lower:]')
          DOCKERHUB_IMAGE_ID=$(printf '%s/docker-images:%s\n' "$DOCKERHUB_USER" "$IMAGE_TAG")

          echo "skip=false" >> "$GITHUB_OUTPUT"
          echo "CODE_PATH=$CODE_PATH" >> "$GITHUB_ENV"
          echo "GHCR_IMAGE_ID=$GHCR_IMAGE_ID" >> "$GITHUB_ENV"
          echo "DOCKERHUB_IMAGE_ID=$DOCKERHUB_IMAGE_ID" >> "$GITHUB_ENV"
          echo "Building ${CODE_PATH}/Dockerfile as ${GHCR_IMAGE_ID} and ${DOCKERHUB_IMAGE_ID}"

      - name: Check OS Env
        if: steps.parse.outputs.skip != 'true'
        run: |
          localectl status
          cat /proc/cpuinfo
          free -m

      - name: Login to GHCR
        if: steps.parse.outputs.skip != 'true'
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Login to Docker Hub
        if: steps.parse.outputs.skip != 'true'
        run: echo "${{ secrets.DOCHUB_PASSWORD }}" | docker login -u "${{ secrets.DOCHUB_USERNAME }}" --password-stdin

      - name: Build Image
        if: steps.parse.outputs.skip != 'true'
        run: docker build -f "${CODE_PATH}/Dockerfile" -t "$GHCR_IMAGE_ID" -t "$DOCKERHUB_IMAGE_ID" .

      - run: docker image ls
        if: steps.parse.outputs.skip != 'true'

      - name: Push Images
        if: steps.parse.outputs.skip != 'true'
        run: |
          docker push "$GHCR_IMAGE_ID"
          docker push "$DOCKERHUB_IMAGE_ID"
